

mixin video(info)
    div
        h4
            a(href=`/videos/${info.id}`)=info.title
        small=info.createdAt
        p=info.description 
        each tag in info.hashtags
            div
                span=tag
        hr